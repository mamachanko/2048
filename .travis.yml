language: python

python:
  - "3.3"
  - "2.7"
  - "2.6"

install:
  - "pip install pytest"
  - "pip install pytest-cov"
  - "pip install coverage"
  - "pip install coveralls"

script:
  - "py.test --tb=native --cov ."

after_success:
  - "coveralls"
